namespace BH.SDK.Models.Enums.Controls
{
    /// <summary>
    /// Which hand the on-screen touch layout is built for - mirrors the whole layout, not one
    /// element.
    /// </summary>
    public enum Handedness : byte
    {
        Right = 0,
        Left = 1,
    }
}
