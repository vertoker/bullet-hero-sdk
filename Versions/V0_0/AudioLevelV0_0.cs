namespace BH.SDK.Versions.V0_0
{
    // ReSharper disable once InconsistentNaming
    
    // Intentionally doesn't have DataVersion

    /// <summary> Audio as it stood at Level v0.0: not a versioned domain yet, so it carries no envelope and
    /// is migrated by hand in <c>LevelV0_0ToV1_0</c>. A frozen snapshot - never edit it to match today's shape. </summary>
    public class AudioLevelV0_0
    {
        
    }
}